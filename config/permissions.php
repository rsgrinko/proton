<?php

declare(strict_types=1);

/**
 * Права приложения.
 *
 * Права ядра (пользователи, роли, журнал, логи, состояние, data.all) объявлены
 * в Permission и здесь не повторяются. Сюда добавляются только свои — вместе
 * с кодом, который их проверяет. Миграция для этого не нужна: в базе хранятся
 * лишь наборы прав у ролей.
 */

use Rsgrinko\Proton\Access\Permission;

Permission::register('notes.view', 'Просмотр заметок', 'Заметки');
Permission::register('notes.manage', 'Управление заметками', 'Заметки');
