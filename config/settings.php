<?php

declare(strict_types=1);

/**
 * Настройки, которые можно менять из панели.
 *
 * Настройки ядра (название, регистрация, почта, вебхуки, уведомления) объявлены
 * в Settings и здесь не повторяются. Сюда добавляются только свои — вместе
 * с кодом, который их читает. Миграция не нужна: в базе хранятся лишь значения.
 *
 * Значение из базы ложится поверх `.env`; пока его там нет, работает то, что
 * в `.env`. Читаются настройки как обычная конфигурация: Config::get('notes.per_user').
 */

use Rsgrinko\Proton\Support\Settings;

Settings::register('notes.per_user', 'Заметок на пользователя', Settings::INT, 'Заметки', [], '0 — без ограничения');
